{
  "name": "firebase",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "deploy-prd": "firebase deploy --only hosting:app-prd",
    "deploy-stg": "firebase deploy --only hosting:app-stg"
  }
}
