{
  "name": "casino-tycoon",
  "version": "1.0.0",
  "main": "index.js",
  "repository": "",
  "author": "huyhn310 <huyhn310@gmail.com>",
  "license": "MIT",
  "scripts": {
    "prepare": "husky install",
    "precommit": "yarn --cwd app build",
    "deploy:prd": "yarn --cwd app build:prd && yarn --cwd firebase deploy:prd",
    "deploy:prd-beta": "yarn --cwd app build:prd-beta && yarn --cwd firebase deploy:prd-beta",
    "deploy:demo": "yarn --cwd app build:demo && yarn --cwd firebase deploy:demo",
    "deploy:stg": "yarn --cwd app build:stg && yarn --cwd firebase deploy:stg",
    "deploy:stg-v2": "yarn --cwd app build:stg-v2 && yarn --cwd firebase deploy:stg-v2",
    "deploy:dev": "yarn --cwd app build:dev && yarn --cwd firebase deploy:dev"
  },
  "lint-staged": {
    "**/*.{js,ts,tsx,json,css,scss,md}": [
      "prettier --write"
    ]
  },
  "devDependencies": {
    "husky": "^8.0.3",
    "lint-staged": "^14.0.1",
    "prettier-plugin-solidity": "^1.1.3"
  }
}
